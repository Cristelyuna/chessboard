let grid='';
let size= 8;
for ( let row=0; row < size; row++){
  
  for (let col=0; col < 8; col++){
    
   if ((row+col) % 2 == 0){
    grid= grid + " ";
  }
    else {
     grid= grid + "#";
    }
  
  }
    grid = grid + "\n";
    
    
  }
  console.log(grid);
